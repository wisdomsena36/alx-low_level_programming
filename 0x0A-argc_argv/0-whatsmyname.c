#include <stdio.h>

/**
 * main - The Entry point.
 * @argc: It counts the array.
 * @argv: The vectors.
 *
 * Return: Always 0 for Success.
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
